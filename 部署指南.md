# GitHub Pages部署指南（Vue 3版）

## 🚀 完整部署流程

本指南将帮助你将Vue 3项目部署到GitHub Pages。

---

## 📋 部署前准备

### 1. 注册必要的账户
- [GitHub](https://github.com) - 代码托管和部署

### 2. 本地环境检查
```bash
# 检查Node.js版本
node -v  # 需要16+

# 检查npm版本
npm -v

# 检查Git版本
git --version
```

---

## 🔧 第一步：创建GitHub仓库

### 1. 登录GitHub
访问 [github.com](https://github.com) 并登录

### 2. 创建新仓库
1. 点击右上角 "+" → "New repository"
2. 填写仓库名称：`FR-site`
3. 选择 "Public"
4. 点击 "Create repository"

---

## 💻 第二步：配置项目

### 1. 修改 `vite.config.ts`
```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import path from 'path'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, 'src')
    }
  },
  base: '/FR-site/'  // 添加这一行，替换为你的仓库名
})
```

### 2. 修改 `package.json`
```json
{
  "name": "fr-site",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://your-username.github.io/FR-site",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  }
}
```

### 3. 修改 `src/config/index.ts`
```typescript
export const config = {
  github: {
    owner: 'your-username',  // 替换为你的GitHub用户名
    repo: 'FR-site',        // 替换为你的仓库名
    branch: 'main'
  }
  // ... 其他配置
}
```

---

## 🔑 第三步：创建GitHub Token

### 1. 访问Token设置
1. 登录GitHub
2. 点击右上角头像 → Settings
3. 左侧菜单：Developer settings → Personal access tokens → Fine-grained tokens
4. 点击 "Generate new token"

### 2. 配置Token权限
1. Token name：`FR-site-admin`
2. Expiration：选择过期时间
3. Repository access：选择 "Only select repositories"
4. 选择你的 `FR-site` 仓库

### 3. 设置权限
在 "Repository permissions" 中：
- **Contents**: Read and write
- **Metadata**: Read-only

### 4. 生成并保存Token
1. 点击 "Generate token"
2. **立即复制Token**（只显示一次）
3. 保存到安全的地方

---

## 🚀 第四步：部署到GitHub Pages

### 方法1：使用gh-pages（推荐）

```bash
# 1. 构建项目
npm run build

# 2. 部署到GitHub Pages
npm run deploy
```

### 方法2：手动部署

```bash
# 1. 构建项目
npm run build

# 2. 进入构建目录
cd dist

# 3. 初始化Git
git init
git add .
git commit -m "Deploy to GitHub Pages"

# 4. 推送到gh-pages分支
git push -u origin main:gh-pages

# 5. 返回项目根目录
cd ..
```

### 方法3：使用GitHub Actions

创建 `.github/workflows/deploy.yml`：
```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
```

---

## ⚙️ 第五步：配置GitHub Pages

### 1. 进入仓库设置
1. 打开你的GitHub仓库
2. 点击 "Settings" 标签
3. 左侧菜单找到 "Pages"

### 2. 配置部署源
1. Source：选择 "Deploy from a branch"
2. Branch：选择 `gh-pages` 分支
3. 文件夹：选择 `/ (root)`
4. 点击 "Save"

### 3. 等待部署
- 通常需要1-2分钟
- 在 "Actions" 标签查看部署状态

### 4. 访问网站
```
https://your-username.github.io/FR-site
```

---

## 🔄 第六步：更新网站

### 更新代码后重新部署
```bash
# 修改代码后
git add .
git commit -m "更新说明"
git push origin main

# 重新部署
npm run deploy
```

### 上传新资料
1. 访问 `/admin/login`
2. 输入GitHub Token
3. 上传资料
4. 资料会自动添加到仓库

---

## 🛠️ 故障排除

### 问题1：页面空白
**原因**：路径配置错误

**解决**：
```typescript
// vite.config.ts
export default defineConfig({
  base: '/FR-site/'  // 确保正确
})
```

### 问题2：资源加载失败
**原因**：base路径问题

**解决**：
```typescript
// 确保base与仓库名一致
base: '/your-repo-name/'
```

### 问题3：路由不工作
**原因**：GitHub Pages不支持History模式

**解决**：使用Hash模式或添加404.html
```typescript
// src/router/index.ts
import { createRouter, createWebHashHistory } from 'vue-router'

const router = createRouter({
  history: createWebHashHistory(),  // 使用Hash模式
  routes
})
```

### 问题4：部署失败
**原因**：gh-pages配置问题

**解决**：
```bash
# 清除缓存
rm -rf node_modules dist
npm install

# 重新部署
npm run deploy
```

---

## 📱 自定义域名（可选）

### 1. 购买域名
推荐域名注册商：
- [Namecheap](https://www.namecheap.com)
- [Cloudflare](https://www.cloudflare.com)
- [阿里云](https://www.aliyun.com)

### 2. 配置DNS
添加CNAME记录：
```
类型: CNAME
名称: www (或 @)
值: your-username.github.io
```

### 3. 配置项目
在 `public` 目录创建 `CNAME` 文件：
```
www.your-domain.com
```

### 4. 配置GitHub
1. 在仓库设置中找到 Pages
2. 填写 Custom domain
3. 勾选 "Enforce HTTPS"

---

## ✅ 完成检查清单

- [ ] GitHub仓库创建完成
- [ ] 项目配置正确（base、homepage）
- [ ] GitHub Token创建并配置
- [ ] 代码推送到GitHub
- [ ] 运行 `npm run deploy`
- [ ] GitHub Pages配置完成
- [ ] 网站可以正常访问
- [ ] 管理员可以登录
- [ ] 资料上传功能正常
- [ ] 资料下载功能正常

---

## 🔗 有用链接

- [GitHub Pages文档](https://docs.github.com/en/pages)
- [Vite部署文档](https://vitejs.dev/guide/static-deploy.html#github-pages)
- [gh-pages npm包](https://www.npmjs.com/package/gh-pages)

---

## 📞 获取帮助

如果遇到问题：
1. 查看GitHub Actions日志
2. 检查浏览器控制台错误
3. 参考Vite和GitHub Pages文档
4. 在GitHub Issues中提问

---

**最后更新**: 2026年6月12日