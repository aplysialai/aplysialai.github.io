# 项目结构说明

## 📁 完整目录结构

```
FR-site/
│
├── 📄 README.md                    # 项目说明文档
├── 📄 开发计划.md                   # 详细开发计划
├── 📄 快速开始.md                   # 快速启动指南
├── 📄 项目结构.md                   # 本文档
├── 📄 .gitignore                   # Git忽略配置
│
├── 📂 client/                      # 前端项目
│   ├── 📂 public/                  # 静态资源
│   │   ├── index.html
│   │   └── favicon.ico
│   │
│   ├── 📂 src/                     # 源代码
│   │   ├── 📂 components/         # 可复用组件
│   │   │   ├── 📂 Layout/        # 布局组件
│   │   │   │   ├── Header.tsx
│   │   │   │   ├── Footer.tsx
│   │   │   │   └── Sidebar.tsx
│   │   │   │
│   │   │   ├── 📂 Auth/          # 认证相关组件
│   │   │   │   ├── LoginForm.tsx
│   │   │   │   └── RegisterForm.tsx
│   │   │   │
│   │   │   ├── 📂 Material/      # 资料相关组件
│   │   │   │   ├── MaterialCard.tsx
│   │   │   │   ├── MaterialList.tsx
│   │   │   │   └── UploadForm.tsx
│   │   │   │
│   │   │   └── 📂 Common/        # 通用组件
│   │   │       ├── Loading.tsx
│   │   │       ├── ErrorBoundary.tsx
│   │   │       └── ProtectedRoute.tsx
│   │   │
│   │   ├── 📂 pages/              # 页面组件
│   │   │   ├── Home.tsx           # 首页
│   │   │   ├── Login.tsx          # 登录页
│   │   │   ├── Register.tsx       # 注册页
│   │   │   ├── MaterialList.tsx   # 资料列表
│   │   │   ├── MaterialDetail.tsx # 资料详情
│   │   │   ├── Upload.tsx         # 上传页面
│   │   │   ├── Profile.tsx        # 个人中心
│   │   │   └── 📂 admin/         # 管理员页面
│   │   │       ├── Dashboard.tsx  # 管理后台
│   │   │       ├── Review.tsx     # 资料审核
│   │   │       └── Users.tsx      # 用户管理
│   │   │
│   │   ├── 📂 services/           # API服务
│   │   │   ├── api.ts            # Axios配置
│   │   │   ├── auth.ts           # 认证API
│   │   │   └── material.ts       # 资料API
│   │   │
│   │   ├── 📂 contexts/           # Context状态管理
│   │   │   └── AuthContext.tsx    # 认证状态
│   │   │
│   │   ├── 📂 hooks/              # 自定义Hooks
│   │   │   ├── useAuth.ts
│   │   │   └── useMaterials.ts
│   │   │
│   │   ├── 📂 types/              # TypeScript类型
│   │   │   ├── user.ts
│   │   │   └── material.ts
│   │   │
│   │   ├── 📂 utils/              # 工具函数
│   │   │   ├── helpers.ts
│   │   │   └── constants.ts
│   │   │
│   │   ├── App.tsx                # 主应用组件
│   │   ├── index.tsx              # 入口文件
│   │   └── index.css              # 全局样式
│   │
│   ├── package.json               # 前端依赖
│   └── tsconfig.json              # TypeScript配置
│
├── 📂 server/                      # 后端项目
│   ├── 📂 controllers/            # 控制器
│   │   ├── authController.js      # 认证控制器
│   │   ├── materialController.js  # 资料控制器
│   │   └── adminController.js     # 管理员控制器
│   │
│   ├── 📂 models/                 # 数据模型
│   │   ├── User.js                # 用户模型
│   │   └── Material.js            # 资料模型
│   │
│   ├── 📂 routes/                 # 路由
│   │   ├── auth.js                # 认证路由
│   │   ├── material.js            # 资料路由
│   │   └── admin.js               # 管理员路由
│   │
│   ├── 📂 middlewares/            # 中间件
│   │   ├── auth.js                # 认证中间件
│   │   ├── upload.js              # 文件上传中间件
│   │   └── errorHandler.js        # 错误处理中间件
│   │
│   ├── 📂 config/                 # 配置
│   │   └── db.js                  # 数据库配置
│   │
│   ├── 📂 utils/                  # 工具函数
│   │   ├── helpers.js
│   │   └── validators.js
│   │
│   ├── server.js                  # 入口文件
│   └── package.json               # 后端依赖
│
└── 📂 docs/                        # 文档目录
    ├── API.md                     # API文档
    └── DEPLOYMENT.md              # 部署文档
```

---

## 📋 文件说明

### 根目录文件

| 文件 | 说明 |
|------|------|
| README.md | 项目概述、功能介绍、快速开始 |
| 开发计划.md | 详细开发计划和时间安排 |
| 快速开始.md | 5分钟快速启动指南 |
| .gitignore | Git忽略的文件和目录 |

### 前端 (client/)

#### 组件 (src/components/)
- **Layout/**: 布局相关组件（头部、底部、侧边栏）
- **Auth/**: 认证相关组件（登录表单、注册表单）
- **Material/**: 资料相关组件（资料卡片、列表、上传表单）
- **Common/**: 通用组件（加载、错误边界、路由守卫）

#### 页面 (src/pages/)
- **公共页面**: 首页、登录、注册
- **用户页面**: 资料列表、详情、上传、个人中心
- **管理员页面**: 后台、审核、用户管理

#### 服务 (src/services/)
- **api.ts**: Axios实例配置和拦截器
- **auth.ts**: 认证相关API（登录、注册）
- **material.ts**: 资料相关API（CRUD操作）

### 后端 (server/)

#### 控制器 (controllers/)
- **authController.js**: 处理用户注册、登录、登出
- **materialController.js**: 处理资料上传、下载、查询
- **adminController.js**: 处理审核、用户管理、统计

#### 模型 (models/)
- **User.js**: 用户数据模型
- **Material.js**: 资料数据模型

#### 路由 (routes/)
- **auth.js**: /api/auth/* 路由
- **material.js**: /api/materials/* 路由
- **admin.js**: /api/admin/* 路由

#### 中间件 (middlewares/)
- **auth.js**: JWT认证验证
- **upload.js**: 文件上传处理（Multer）
- **errorHandler.js**: 统一错误处理

---

## 🔗 文件依赖关系

### 前端依赖流
```
pages/ → components/ → services/ → api.ts
                ↓
           contexts/ (状态管理)
                ↓
           hooks/ (自定义逻辑)
```

### 后端依赖流
```
routes/ → controllers/ → models/
              ↓
         middlewares/ (认证、验证)
              ↓
         utils/ (工具函数)
```

---

## 📝 命名规范

### 文件命名
- 组件文件: PascalCase (如 `MaterialCard.tsx`)
- 工具文件: camelCase (如 `helpers.ts`)
- 配置文件: camelCase (如 `db.js`)
- 类型文件: camelCase (如 `user.ts`)

### 目录命名
- 组件目录: PascalCase (如 `Material/`)
- 其他目录: camelCase (如 `services/`)

### 变量命名
- 组件名: PascalCase (如 `MaterialCard`)
- 函数名: camelCase (如 `getUserById`)
- 常量名: UPPER_SNAKE_CASE (如 `API_BASE_URL`)

---

## 🎯 创建文件的顺序

建议按以下顺序创建文件：

### 第一阶段：基础结构
1. 根目录配置文件
2. 前端项目初始化
3. 后端项目初始化

### 第二阶段：后端核心
1. 数据库配置
2. 数据模型
3. 控制器
4. 路由
5. 中间件

### 第三阶段：前端核心
1. 基础组件
2. 页面组件
3. API服务
4. 状态管理

### 第四阶段：完善功能
1. 管理员功能
2. 优化和测试
3. 文档完善

---

## 💡 提示

1. **模块化**: 每个功能模块独立，便于维护
2. **可复用**: 通用组件放在Common目录
3. **类型安全**: 使用TypeScript确保类型正确
4. **清晰命名**: 文件名清晰表达其用途
5. **文档完善**: 每个模块添加必要注释

---

**注意**: 实际开发中可根据需要调整结构，以上为推荐组织方式。